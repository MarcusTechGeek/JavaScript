// Outputting the protocol (e.g., "http:" or "https:") of the current page's URL.
console.log(window.location.protocol);

// Outputting the complete URL of the current page.
console.log(window.location.href);
