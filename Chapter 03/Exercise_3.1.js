// Creating an array named myList with three items: 'Milk', 'Bread', 'Apples'
const myList = ["Milk", "Bread", "Apples"];

// Logging the length of the myList array (number of elements)
console.log(myList.length);

// Updating the element at index 1 to 'Bananas'
myList[1] = "Bananas";

// Logging the modified myList array
console.log(myList);

