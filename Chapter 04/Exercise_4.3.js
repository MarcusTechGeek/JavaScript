// Declaring a constant variable id with the value true
const id = true;

// Using a ternary operator to assign a value to the message variable based on the id condition
const message = (id) ? "Allowed In" : "Denied Entry";

// Logging the message to the console
console.log(message);
